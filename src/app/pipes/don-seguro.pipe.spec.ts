import { DonSeguroPipe } from './don-seguro.pipe';

describe('DonSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new DonSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
